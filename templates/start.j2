#!/bin/bash
export DISPLAY=:1;
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
java {{selenium_gecko_driver}} -cp $DIR/lib/* org.openqa.grid.selenium.GridLauncherV3 -role  node -nodeConfig $DIR/{{item.username}}_{{item.profile}}.conf -servlets NodeServlet